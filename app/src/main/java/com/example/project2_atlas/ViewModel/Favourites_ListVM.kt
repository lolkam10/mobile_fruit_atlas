package com.example.project2_atlas.ViewModel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class Favourites_ListVM (application: Application) : AndroidViewModel(application) {

}